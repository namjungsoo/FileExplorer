apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion '25.0.3'

    defaultConfig {
        applicationId 'com.duongame'
        buildConfigField "boolean", "SHOW_AD", "true"
        minSdkVersion 14
        targetSdkVersion 25
        versionCode 2
        versionName "1.0.1"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    flavorDimensions("project", "edition")

    productFlavors {
        main {
            dimension "edition"
        }

        // 파일탐색기
        file {
            buildConfigField "String", "BANNER_ID", "\"ca-app-pub-5576037828251153/8260818625\""
            buildConfigField "String", "POPUP_ID", "\"ca-app-pub-5576037828251153/2214285028\""
            buildConfigField "String", "INTERSTITIAL_ID", "\"ca-app-pub-5576037828251153/9737551820\""
            applicationIdSuffix '.file'
            dimension "project"
        }
        // 코믹제트
        comicz {
            buildConfigField "String", "BANNER_ID", "\"ca-app-pub-5576037828251153/8260818625\""
            buildConfigField "String", "POPUP_ID", "\"ca-app-pub-5576037828251153/2214285028\""
            buildConfigField "String", "INTERSTITIAL_ID", "\"ca-app-pub-5576037828251153/9737551820\""
            applicationIdSuffix '.comicz'
            dimension "project"
        }

        // 유료
        pro {
            buildConfigField "boolean", "SHOW_AD", "false"
            //resValue "string", "app_name", "@string/app_name_pro"
            addManifestPlaceholders([APP_NAME: "@string/app_name_pro"])
            dimension "edition"
        }
        // 무료
        free {
            applicationIdSuffix '.free'
            //resValue "string", "app_name", "@string/app_name_free"
            addManifestPlaceholders([APP_NAME: "@string/app_name_free"])
            dimension "edition"
        }

    }
    variantFilter { variant ->
        if (variant.getFlavors().get(1).name.equals('main')) {
            variant.setIgnore(true);
        }
        if (variant.getFlavors().get(1).name.equals('pro')) {
            variant.setIgnore(true);
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')

    //android
    compile 'com.android.support:appcompat-v7:25.3.1'
    compile 'com.android.support:design:25.3.1'

    //zip
    compile 'net.lingala.zip4j:zip4j:1.2.4'
    //encoding
    compile 'com.googlecode.juniversalchardet:juniversalchardet:1.0.3'

    //GIF
    compile 'com.felipecsl:gifimageview:2.1.0'
    compile 'com.github.chrisbanes:PhotoView:2.0.0'

    //GLIDE
    compile 'com.github.bumptech.glide:glide:4.1.1'
    annotationProcessor 'com.github.bumptech.glide:compiler:4.1.1'

    //firebase
    compile 'com.google.firebase:firebase-crash:10.0.1'
    compile 'com.google.firebase:firebase-core:10.0.1'

    compile 'com.google.android.gms:play-services-analytics:10.0.1'
    compile 'com.google.android.gms:play-services-ads:10.0.1'
}

// ADD THIS AT THE BOTTOM
apply plugin: 'com.google.gms.google-services'
